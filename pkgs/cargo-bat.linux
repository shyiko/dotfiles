#!/bin/bash
#@pkg.file=$HOME/.cargo/bin/bat
set -e

# sudo apt install -o Dpkg::Options::="--force-overwrite" bat # conflict with ripgrep over /usr/.crates2.json
# sudo ln -sf $(which batcat) /usr/local/bin/bat

cargo install --tag=v0.15.4 bat
